#include "../headers/selection_sort.h"

int main(int argc, char *argv[])
{
  // if (argc != 2)
  // {
  //   printf("Expected parameter to function.");
  //   exit(1);
  // }

  // bigNum tam = atoi(argv[1]);
  int i;
  bigNum tam = 100;

  create_random_ordered_file(100);

  return 0;
}