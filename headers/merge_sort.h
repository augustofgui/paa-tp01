#ifndef MERGE_SORT_H
#define MERGE_SORT_H

#include "./utils.h"

void mergeSort(bigNum numbers[], bigNum L, bigNum R);

void merge(bigNum numbers[], bigNum L, bigNum M, bigNum R);

#endif // MERGE_SORT_H